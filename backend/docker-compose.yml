services:
  tooth-backend:
    build: .
    container_name: tooth-backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./storage:/app/storage
