# 模型卡与使用边界

## 龋齿 YOLOv8s

训练数据为口内可见光照片；两个来源标签合并为单类 `caries`。测试集包含 319 张有效图像、973 个实例。该模型输出目标框，不提供龋齿分级或牙位编号。

## 牙结石 U-Net++ v3

训练目标为龈上牙结石的像素分割。v3 在去除完全重复泄漏的基础上，将 83 组“同图不同疾病部分标注”中的有效牙结石掩膜合并进训练集。固定测试集结果为 Dice 0.392、IoU 0.244、Precision 0.302、Recall 0.557。

模型尚不适合作为独立临床诊断系统；应将叠加区域理解为需要人工复核的候选沉积区域。
